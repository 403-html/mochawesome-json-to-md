env:
  node: true
  es2021: true
extends:
  - eslint:recommended
parserOptions:
  ecmaVersion: latest
  sourceType: module
rules:
  curly: error
  default-case: error
  eqeqeq:
    - error
    - always
  no-console:
    - warn
    - allow:
        - warn
        - error
  no-implicit-coercion: error
  no-redeclare: error
  no-shadow: error
  no-unused-vars:
    - error
    - argsIgnorePattern: '^_'
  no-var: error
  prefer-const: error
  consistent-return: error
  complexity:
    - warn
    - max: 10
  max-lines-per-function:
    - warn
    - max: 60
      skipBlankLines: true
      skipComments: true
  max-depth:
    - warn
    - 3
overrides:
  - files:
      - "tests/**/*"
    env:
      jest: true
    rules:
      # describe() blocks legitimately grow past the general function-length
      # limit as coverage increases; the limit stays meaningful for source code.
      max-lines-per-function: off
